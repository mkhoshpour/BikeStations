# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BikeStations' do
  use_frameworks!
	pod 'GoogleMaps'
	pod 'PKHUD'

  # Pods for BikeStations

  target 'BikeStationsTests' do
    
  end

end
